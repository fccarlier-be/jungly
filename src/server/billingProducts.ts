/**
 * Identifiant du produit "achat unique" cree dans Play Console pour
 * debloquer l'offre hebergee (jungly-app.fcold.org). Provisoire tant que
 * le produit n'existe pas reellement -- a synchroniser avec Play Console
 * une fois cree (Phase 6).
 */
export const HOSTED_ACCESS_PRODUCT_ID = "hosted_access_lifetime";
